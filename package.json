{
  "name": "expense-budget-visualizer",
  "version": "1.0.0",
  "description": "Client-side expense & budget visualizer",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "tests/**/*.test.js"
    ]
  }
}
